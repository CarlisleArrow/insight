# EXAMPLE ONLY — do NOT commit real secrets (§1.3, §12.3). Create the real
# Secrets out-of-band (sealed-secrets / vault) and reference them in the
# Deployment. Values below are placeholders.
apiVersion: v1
kind: Secret
metadata:
  name: cp-keycloak
  namespace: control-plane
type: Opaque
stringData:
  client-secret: "REPLACE_ME"          # insight client secret (§2.1)
---
apiVersion: v1
kind: Secret
metadata:
  name: cp-postgres
  namespace: control-plane
type: Opaque
stringData:
  password: "REPLACE_ME"               # cp_app role password (§4.1)
---
apiVersion: v1
kind: Secret
metadata:
  name: cp-clickhouse
  namespace: control-plane
type: Opaque
stringData:
  username: "cp_app"                    # dedicated CH user (§12.3 — not the empty default)
  password: "REPLACE_ME"
---
apiVersion: v1
kind: Secret
metadata:
  name: cp-minio
  namespace: control-plane
type: Opaque
stringData:
  access-key: "REPLACE_ME"             # replace minioadmin defaults (§13.2)
  secret-key: "REPLACE_ME"
