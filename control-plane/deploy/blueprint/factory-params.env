# Per-factory blueprint parameters (§19.5). Copy + edit per site.
INSIGHT_ROLE=factory
INSIGHT_FACTORY_ID=fab-a
INSIGHT_VERSION=bp-2026.06
# HQ ingest endpoint (the hybrid instance's public URL). Empty disables reporting.
CP_FEDERATION_TOWER_ENDPOINT=http://hq.insight.example.com
# Must match the HQ's token; gates /federation-ingest/*.
CP_FEDERATION_SHARED_TOKEN=change-me
